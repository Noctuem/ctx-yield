# ctx-yield — session handoff

- **Spec:** `notes/projects/specs/current/ctx-yield.spec.md` in the SIA hub
  (`~\SIA`). At scaffold time the spec sits in `specs/new/`; it moves to
  `current/` on registration.
- **Where we are:** scaffolded (skeleton + packaging only); build not started.
- **Next:** `/sia:build` from this folder — Modules 1–2 (`corpus`, `tokens`)
  have no dependencies and open the graph.
- **Pointers:** `TODO.md` (backlog), `Session_Log.md` (append-only session
  record).
