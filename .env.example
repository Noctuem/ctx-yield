# Optional. Used only by `ctx-yield scan --exact` to call the free
# /v1/messages/count_tokens endpoint. Without it, ctx-yield falls back to the
# calibrated offline heuristic (and prints its error bar).
ANTHROPIC_API_KEY=
